//Substraction function
function minus(x, y) {
  return x - y;
};

//Addition function
function add(x, y) {
  return x + y;
};

//Division function
function dev(x, y) {
  return x / y;
};

//Multiplication function
function mult(x, y) {
  return x * y;
};