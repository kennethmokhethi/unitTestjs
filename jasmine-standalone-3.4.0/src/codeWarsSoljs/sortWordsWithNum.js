function sortWords(words){

       //Changing the string to a word array
       var arrWords=words.split(" ");

        for(let a=0;a<arrWords.length;a++)
        {
           //Extracting numbers from each element in the strings
            var num=/\d+/g;   
             for(let charArr of arrWords[a])
             {
                 
               
             };
        };

     

    
    

    return arrNums;
    



};